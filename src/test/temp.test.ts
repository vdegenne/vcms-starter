import {expect} from 'chai';
import {readFileSync} from 'fs';
import * as yaml from 'js-yaml';
/* import {ConfigOptions} from 'vcms';

suite('Yaml', () => {
  test('it reads files', async () => {
    const content: ConfigOptions = <ConfigOptions>yaml.safeLoad(
        readFileSync(process.cwd() + '/.vcms.yml').toString());
    expect(content.database).to.be.ok;
  });
}); */
